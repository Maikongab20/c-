#include <stdio.h>
#include <string.h>
# define TF 20

int main(){
	int i,contas[TF];
	char nomes[TF][50];
	float saldo[TF];
	
	for(i=0; i<TF; i++){
		printf("numero da conta");
		scanf("%d",&contas[i]);
		printf("Nome do titular");
		fflush(stdin);
		get(nomes[i]);
		printf("saldo da conta");
		scanf("%d",&csaldo[i]);
	}
	
	do{
		
	}while();
}
